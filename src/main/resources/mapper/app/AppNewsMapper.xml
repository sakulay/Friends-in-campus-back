<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youlai.boot.app.model.mapper.AppNewsMapper">

    <select id="getAppNewsPage" resultType="com.youlai.boot.app.model.vo.AppNewsVO">
        SELECT *
        FROM app_news
        <where>
        </where>
        ORDER BY date desc
    </select>

</mapper>