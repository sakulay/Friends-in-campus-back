<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youlai.boot.app.model.mapper.AppFriendMapper">

    <!-- 获取用户好友关系分页列表 -->
    <select id="getAppFriendPage" resultType="com.youlai.boot.app.model.vo.AppFriendVO">
        SELECT
                id,
                user_id,
                friend_id,
                status,
                create_time,
                update_time,
                is_deleted
        FROM
            app_friend
        <where>
        </where>
    </select>

    <!-- 根据学生ID查询好友列表，支持模糊查询  -->
    <select id="getAppFriendListByStudentId" resultType="com.youlai.boot.app.model.vo.FriendSimpleVO">
        SELECT
            u.student_id,
            u.nickname,
            u.gender,
            u.bio,
            u.avatar,
            f.status
        FROM
            app_friend f
        LEFT JOIN
            app_user_profile u ON f.friend_id = u.student_id
        WHERE
            f.user_id = #{studentId}
            <if test="keyword != null and keyword != ''">
                AND u.nickname LIKE CONCAT('%', #{keyword}, '%')
            </if>
        ORDER BY
            f.create_time DESC
    </select>

    <select id="getAppFriendRelationship" resultType="java.lang.Integer">
        SELECT
            status
        FROM
            app_friend
        WHERE
            user_id = #{studentId}
            AND friend_id = #{friendId}
    </select>
    <select id="getAppFriendInfo" resultType="com.youlai.boot.app.model.vo.FriendSimpleVO">
        SELECT
            u.student_id,
            u.nickname,
            u.gender,
            u.bio,
            u.avatar,
            f.status
        FROM
            app_user_profile u
                LEFT JOIN app_friend f
                    ON
                        u.student_id = f.user_id
                    AND f.friend_id = #{studentId}
        WHERE
            u.student_id = #{friendId}
                AND
            u.is_deleted = 0
    </select>

</mapper>